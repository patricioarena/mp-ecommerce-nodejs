<!DOCTYPE html>
<html
    class="supports-animation supports-columns svg no-touch no-ie no-oldie no-ios supports-backdrop-filter as-mouseuser"
    lang="en-US">

<head>
    <script src="https://sdk.mercadopago.com/js/v2"></script>
    <script src="https://www.mercadopago.com/v2/security.js" view="home"></script>
</head>

<body>

    <div class="row" style="justify-content: center;">

        {{#each items}}

        <div class="card text-center" style="width: 16rem; margin: 10px;">
            <div class="card-header" style="background-color: rgba(0,0,0,0) !important;">
                <img src={{ img }} class="card-img-top" alt={{ title }} width="200" height="200">
            </div>
            <div class="card-body">
                <span class="card-title" style="font-weight: 600;">{{ title }}</span><br>
                <span class="card-text" style=" font-size: 14px; font-weight: 400; line-height: 1.3;">{{ description
                    }}</span>
            </div>
            <div class="card-footer bg-transparent" style="border-top: 1px solid rgba(0,0,0,0) !important;">
                <h5 class="card-text">{{ fraction }}</h5>
            </div>
            <div class="card-footer bg-transparent">
                <form action="/detail" method="post">
                    <input type="hidden" name="id" id="id" value={{ id }}>
                    <textarea type="hidden" name="description" id="description"
                        style="display:none;">{{ description }}</textarea>
                    <input type="hidden" name="img" id="img" value={{ img }}>
                    <input type="hidden" name="title" id="title" value={{ title }}>
                    <input type="hidden" name="price" id="price" value={{ price }}>
                    <input type="hidden" name="fraction" id="fraction" value={{ fraction }}>
                    <input type="hidden" name="unit" id="unit" value={{ unit }}>
                    <button type="submit" class="btn btn-primary" formmethod="post">Comprar</button>
                </form>
            </div>
        </div>

        {{else}}

        <div class="card mx-auto">
            <div class="card-body">
                <h1>Hello!</h1>
                <p clsss="lead">There are not products yet.</p>
            </div>
        </div>

        {{/each}}

    </div>

</body>

</html>